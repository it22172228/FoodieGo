module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Include all source files
    "./public/index.html",        // Include the main HTML file
  ],
  theme: {
    extend: {}, // Extend default Tailwind theme if needed
  },
  plugins: [], // No additional plugins for simplicity
};
